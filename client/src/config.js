// API Configuration
const API_BASE_URL = process.env.REACT_APP_API_URL || 'https://hotelserver.aisoft.it.com'; //'https://hotel-menumanagement.onrender.com';

export const config = {
  API_BASE_URL,
  // Add other configuration options here
};

export default config; 